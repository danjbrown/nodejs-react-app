services:

  server:
    build:
      context: ./src/server
      dockerfile: Dockerfile
    ports:
      - 3000:3000

  frontend:
    restart: on-failure
    build:
      context: ./src/client
      dockerfile: Dockerfile.prod
    ports:
      - 5173:5173
    depends_on:
      - server
