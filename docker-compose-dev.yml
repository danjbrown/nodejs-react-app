services:

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - 3000:3000

  frontend:
    restart: on-failure
    build:
      context: ./client
      dockerfile: Dockerfile.dev
    ports:
      - 5173:5173
    depends_on:
      - server
